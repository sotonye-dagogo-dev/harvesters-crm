# System Architecture

> **Metadata**
> - last-updated-by: (set on first run)
> - last-verified-against-code: (set when architecture matches actual code)
> - staleness-policy: re-verify before trusting if any architecture-affecting commits have been made since last-verified-against-code

> **Overview:** How the system is structured — layers, modules, data flow, and configuration. Agents designing or changing structure must read this first.

---

## Architecture Diagram

```
[Generated by bootstrap-project or architect role]

Example:
Client (Browser / Mobile)
        ↓
    Presentation Layer
        ↓
    API / Gateway Layer
        ↓
    Service Layer (business logic)
        ↓
    Data Access Layer
        ↓
    Data Store(s) / External APIs
```

---

## Module Breakdown

| Module | Responsibility | Key Files | Dependencies |
|--------|---------------|-----------|--------------|
| [module] | [what it does] | [files] | [what it depends on] |

---

## Data Flow

### Standard Request Flow
```
[describe the path a request takes through layers]
```

### Authentication Flow
```
[describe auth path]
```

### Data Persistence Flow
```
[describe how data is saved/retrieved]
```

---

## Configuration Points

| Config Key | Purpose | Location | Default |
|-----------|---------|----------|---------|
| [key] | [what it controls] | [.env / config file] | [value] |

All config points listed here should follow the fallback discipline from `standards/engineering-principles.md` §1 and §3 — every config-driven value must have a documented, safe fallback so the system degrades gracefully if the value is missing or malformed.

---

## Tech Stack

| Layer | Technology | Version |
|-------|-----------|---------|
| Frontend | [e.g. React / Next.js] | [version] |
| Backend | [e.g. Node.js / Express] | [version] |
| Database | [e.g. PostgreSQL] | [version] |
| Auth | [e.g. JWT] | — |

---

## Known Constraints & Technical Debt

- [List constraints here]

---

## Architecture History

See `memory/architecture-history.md` for full chronology.
